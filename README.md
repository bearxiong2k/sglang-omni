# OmniTyper usability comparison

User-captured screenshots of the original app and the conservative PR review app on September 19, 2026. PNG files are copied unchanged; user-captures.json records provenance and checksums.

Pairs: visible console title bar, settings controls, optional model retention, and notification placement. Window activation state, shortcut bindings, selected modes and notice content differ between some captures; these are not controlled before/after tests. Behavioral claims are verified separately.
